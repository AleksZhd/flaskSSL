Ubuntu 24 only!
0. Install app:
	sudo apt install net-tools tree python3-venv
1. Create virtual environment:
	python3 -m venv .venv
2. Change to venv:
	source .venv/bin/activate
3. Install all packages:
	pip install -r requirments.txt
4. Start server:
	bash start
5. SSH tunnel to server:
	run on locale machine:
	ssh -p 22 user@195.1.2.3 -L 127.0.0.1:5000:127.0.0.1:5000
